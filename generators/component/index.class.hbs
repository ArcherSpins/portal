// @flow
import React, { Component } from 'react';

import styles from './{{ properCase name }}.module.scss';

type Props = {||};

export default class {{ properCase name }} extends Component<Props> {
    render() {
    return (
    <div className={styles.{{ kebabCase name }} }>
        {/* TODO: write rest of ${name} container */}
    </div>
    )
    }
    };
