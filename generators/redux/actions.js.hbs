// @flow

import type { {{properCase name}}Actions } from './types';
import * as {{ camelCase name}}Types from './constants';

export const exampleAction = (): {{properCase name}}Actions => ({
    type: {{camelCase name}}Types.{{constantCase name}}_REQUEST,
    payload: null,
});