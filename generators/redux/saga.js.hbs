// @flow

// import { put, takeEvery, call } from 'redux-saga/effects';
import type { Saga } from 'redux-saga';
import type { {{properCase name}}Actions } from './types';

function* {{ camelCase name}}Saga(action: {{properCase name}}Actions): Saga<void> {

    try {
        // place your code here
    } catch(err) {
        // and here
    }
    }


    export default function* watch{{properCase name}}(): Saga<void> {
        // yield takeLatest('SOME_ACTION', {{ camelCase name}}Saga);
    };