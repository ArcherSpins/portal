import styled from 'styled-components';
import { Heading } from '../../components/shared/styled';

export const Title = styled(Heading)`
  margin-right: 20px;
`;

export const Label = styled.label``;
