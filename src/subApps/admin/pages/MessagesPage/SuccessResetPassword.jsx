import React from 'react';
import { Message, Container } from './styled';

export default () => (
  <Container>
    <Message>We have accepted your application, we will send you an e-mail</Message>
  </Container>
);
