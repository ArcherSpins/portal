{
  "name": "internal-portal",
  "version": "1.0.0",
  "main": "packages/webapp/src/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build-ui": "cd packages/ui-kit && yarn build && lerna bootstrap && cd ../../",
    "start": "cd packages/webapp && yarn start",
    "build": "cd packages/webapp && yarn build && mv build ../../" 
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.18.3"
  }
}
